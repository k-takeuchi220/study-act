name: Sample

on:
  push:

jobs:
  sample:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: terraform check
        run: terraform fmt -recursive -check
